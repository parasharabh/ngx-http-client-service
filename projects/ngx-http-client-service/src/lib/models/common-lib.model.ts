export class URLQueryParamType {
  query_param_name: string;
  query_param_value: string;
}

export class TimeFormat {
  format: string;
  duration: number;
  full_time_format: string; // will provide full format in n day(s), HH:MM:SS
}
